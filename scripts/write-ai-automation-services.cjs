const fs = require('node:fs')
const path = require('node:path')
const root = path.join(__dirname, '..')
const automationDir = path.join(root, 'src/services/ai-automation')
fs.mkdirSync(automationDir, { recursive: true })
const eSeal = [
  "export type SealParty = 'partyA' | 'partyB'",
  'export const createElectronicSealData = (label: string, party: SealParty = \"partyA\") => {',
  '  const color = party === \"partyA\" ? \"#dc2626\" : \"#2563eb\"',
  '  const svg = `<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"220\" height=\"220\" viewBox=\"0 0 220 220\"><circle cx=\"110\" cy=\"110\" r=\"96\" fill=\"none\" stroke=\"${color}\" stroke-width=\"6\"/><text x=\"110\" y=\"110\" text-anchor=\"middle\" fill=\"${color}\" font-size=\"16\" font-weight=\"700\">${label.slice(0, 8)}</text><text x=\"110\" y=\"140\" text-anchor=\"middle\" fill=\"${color}\" font-size=\"12\">电子签章</text></svg>`',
  '  return `data:image/svg+xml;base64,${btoa(unescape(encodeURIComponent(svg)))}`',
  '}',
].join('\n')

const businessFlow = [
  "import { createContract, signContract, submitContract, type ContractItem } from '@/services/eContract'",
  "import { runWorkflowAction, startWorkflow, type WorkflowInstance } from '@/services/approvalWorkflow'",
  "import { getApiErrorMessage } from '@/utils/request'",
  "import { createElectronicSealData } from './e-seal'",
  'export type BusinessClosureStep = { key: string; label: string; status: \"done\" | \"error\"; detail: string }',
  'export type BusinessClosureResult = { keyword: string; approvalId: number; approvalCode: string; approvalStatus: string; contractId: number; contractCode: string; contractStatus: string; sealsApplied: string[]; steps: BusinessClosureStep[]; completedAt: number }',
  'export type BusinessClosureInput = { keyword: string; title?: string; amount?: number; summary?: string; operator?: string; onStep?: (step: BusinessClosureStep) => void }',
  'const unwrap = <T>(response: { data: T }) => response.data',
  'const pushStep = (steps: BusinessClosureStep[], step: BusinessClosureStep, onStep?: (step: BusinessClosureStep) => void) => { steps.push(step); onStep?.(step) }',
  'export const runBusinessClosureFlow = async (input: BusinessClosureInput): Promise<BusinessClosureResult> => {',
  "  const operator = input.operator?.trim() || 'FlowAgent AI'",
  '  const keyword = input.keyword.trim()',
  '  const title = input.title?.trim() || `${keyword} 产品立项审批`',
  '  const amount = input.amount ?? 50000',
  "  const reason = input.summary?.trim() || `基于「${keyword}」AI 工作流产出结果，自动发起立项审批并完成电子合同签署。`",
  '  const steps: BusinessClosureStep[] = []',
  '  let workflow: WorkflowInstance',
  '  try {',
  '    workflow = unwrap(await startWorkflow({ title, type: \"AI 立项审批\", amount, reason, applicant: operator }))',
  '    pushStep(steps, { key: \"approval_start\", label: \"发起审批\", status: \"done\", detail: `${workflow.code} · ${workflow.currentStep}` }, input.onStep)',
  '    workflow = unwrap(await runWorkflowAction({ id: workflow.id, action: \"approve\", operator: \"AI 审批官\", comment: \"AI 自动审批通过\" }))',
  '    pushStep(steps, { key: \"approval_pass\", label: \"自动审批通过\", status: \"done\", detail: workflow.currentStep }, input.onStep)',
  '  } catch (error) {',
  "    const message = getApiErrorMessage(error, '审批流程调用失败')",
  '    pushStep(steps, { key: \"approval_error\", label: \"审批流程\", status: \"error\", detail: message }, input.onStep)',
  '    throw new Error(message)',
  '  }',
  '  const sealsApplied: string[] = []',
  '  let contract: ContractItem',
  '  try {',
  '    contract = unwrap(await createContract({ title: `${keyword} 合作框架协议`, counterparty: \"FlowAgent 战略合作方\", amount, content: reason, createdBy: operator }))',
  '    pushStep(steps, { key: \"contract_create\", label: \"生成电子合同\", status: \"done\", detail: contract.code }, input.onStep)',
  '    contract = unwrap(await submitContract({ id: contract.id, operator, comment: \"AI 自动提交签署\" }))',
  '    pushStep(steps, { key: \"contract_submit\", label: \"提交签署\", status: \"done\", detail: contract.currentStep }, input.onStep)',
  "    contract = unwrap(await signContract({ id: contract.id, signerRole: 'partyA', signatureData: createElectronicSealData('FlowAgent', 'partyA'), operator: 'FlowAgent 甲方' }))",
  "    sealsApplied.push('partyA')",
  '    pushStep(steps, { key: \"seal_a\", label: \"甲方电子签章\", status: \"done\", detail: contract.currentStep }, input.onStep)',
  "    contract = unwrap(await signContract({ id: contract.id, signerRole: 'partyB', signatureData: createElectronicSealData('战略合作方', 'partyB'), operator: 'FlowAgent 乙方' }))",
  "    sealsApplied.push('partyB')",
  '    pushStep(steps, { key: \"seal_b\", label: \"乙方电子签章\", status: \"done\", detail: `${contract.currentStep} · ${contract.statusLabel}` }, input.onStep)',
  '  } catch (error) {',
  "    const message = getApiErrorMessage(error, '合同签署调用失败')",
  '    pushStep(steps, { key: \"contract_error\", label: \"电子合同\", status: \"error\", detail: message }, input.onStep)',
  '    throw new Error(message)',
  '  }',
  '  return { keyword, approvalId: workflow.id, approvalCode: workflow.code, approvalStatus: workflow.status, contractId: contract.id, contractCode: contract.code, contractStatus: contract.status, sealsApplied, steps, completedAt: Date.now() }',
  '}',
].join('\n')

const scheduler = [
  "import { runProductWorkflowAgent, type WorkflowAgentProgress } from '@/services/ai-agent'",
  "import { getWorkflowModelConfig } from '@/services/ai'",
  "import { runBusinessClosureFlow, type BusinessClosureStep } from './business-flow'",
  "export type AutomationScheduleType = 'daily' | 'weekly' | 'interval'",
  "export type AutomationTaskType = 'product_workflow' | 'business_closure' | 'full_pipeline'",
  'export type AutomationTask = { id: string; name: string; enabled: boolean; taskType: AutomationTaskType; scheduleType: AutomationScheduleType; timeOfDay: string; weekday: number; intervalMinutes: number; keyword: string; enableBusinessClosure: boolean; lastRunAt?: number; nextRunAt?: number; createdAt: number }',
  "export type AutomationRunLog = { id: string; taskId: string; taskName: string; taskType: AutomationTaskType; keyword: string; status: 'success' | 'error'; message: string; startedAt: number; finishedAt: number }",
  "const TASKS_KEY = 'flowagent-automation-tasks'",
  "const LOGS_KEY = 'flowagent-automation-logs'",
  'const readJson = <T>(key: string, fallback: T): T => { if (typeof window === \"undefined\") return fallback; try { const raw = window.localStorage.getItem(key); return raw ? JSON.parse(raw) as T : fallback } catch { return fallback } }',
  'const writeJson = (key: string, value: unknown) => { if (typeof window !== \"undefined\") window.localStorage.setItem(key, JSON.stringify(value)) }',
  'export const computeNextRunAt = (task: Pick<AutomationTask, \"scheduleType\" | \"timeOfDay\" | \"weekday\" | \"intervalMinutes\">, from = Date.now()) => {',
  "  if (task.scheduleType === 'interval') return from + Math.max(5, task.intervalMinutes || 60) * 60 * 1000",
  '  const parts = (task.timeOfDay || \"09:00\").split(\":\"); const h = Number.parseInt(parts[0] || \"9\", 10) || 9; const m = Number.parseInt(parts[1] || \"0\", 10) || 0;',
  '  const next = new Date(from); next.setSeconds(0, 0); next.setHours(h, m, 0, 0)',
  "  if (task.scheduleType === 'daily') { if (next.getTime() <= from) next.setDate(next.getDate() + 1); return next.getTime() }",
  '  let offset = ((task.weekday ?? 1) - next.getDay() + 7) % 7; if (offset === 0 && next.getTime() <= from) offset = 7; next.setDate(next.getDate() + offset); return next.getTime()',
  '}',
  'export const getAutomationTasks = () => readJson<AutomationTask[]>(TASKS_KEY, [])',
  'export const saveAutomationTasks = (tasks: AutomationTask[]) => writeJson(TASKS_KEY, tasks)',
  'export const getAutomationLogs = () => readJson<AutomationRunLog[]>(LOGS_KEY, [])',
  'export const appendAutomationLog = (log: AutomationRunLog) => writeJson(LOGS_KEY, [log, ...getAutomationLogs()].slice(0, 30))',
  'export const createAutomationTask = (payload: Omit<AutomationTask, \"id\" | \"createdAt\" | \"lastRunAt\" | \"nextRunAt\">) => { const task: AutomationTask = { ...payload, id: `task-${Date.now()}`, createdAt: Date.now(), nextRunAt: computeNextRunAt(payload) }; saveAutomationTasks([task, ...getAutomationTasks()]); return task }',
  'export const updateAutomationTask = (id: string, patch: Partial<AutomationTask>) => { const tasks = getAutomationTasks().map((task) => task.id === id ? { ...task, ...patch, nextRunAt: computeNextRunAt({ ...task, ...patch }) } : task); saveAutomationTasks(tasks); return tasks.find((item) => item.id === id) || null }',
  'export const deleteAutomationTask = (id: string) => saveAutomationTasks(getAutomationTasks().filter((task) => task.id !== id))',
  'export const getDueAutomationTasks = (now = Date.now()) => getAutomationTasks().filter((task) => task.enabled && task.nextRunAt !== undefined && task.nextRunAt <= now)',
  'export type AutomationRunHandlers = { onProgress?: (progress: WorkflowAgentProgress) => void; onBusinessStep?: (step: BusinessClosureStep) => void }',
  'export const runAutomationTask = async (task: AutomationTask, handlers: AutomationRunHandlers = {}) => {',
  '  const startedAt = Date.now()',
  '  try {',
  "    if (task.taskType === 'business_closure') { const result = await runBusinessClosureFlow({ keyword: task.keyword, onStep: handlers.onBusinessStep }); return finishRun(task, startedAt, 'success', `商务闭环完成：${result.contractCode}`) }",
  '    const workflow = await runProductWorkflowAgent(task.keyword, { modelConfig: getWorkflowModelConfig(), enableBusinessClosure: task.enableBusinessClosure || task.taskType === \"full_pipeline\", onProgress: handlers.onProgress, onBusinessStep: handlers.onBusinessStep })',
  '    const suffix = workflow.businessClosure?.contractCode ? `，合同 ${workflow.businessClosure.contractCode} 已签章` : \"\"',
  "    return finishRun(task, startedAt, 'success', `工作流完成：${task.keyword}${suffix}`)",
  '  } catch (error) { return finishRun(task, startedAt, \"error\", error instanceof Error ? error.message : \"自动化任务失败\") }',
  '}',
  'const finishRun = (task: AutomationTask, startedAt: number, status: AutomationRunLog[\"status\"], message: string) => {',
  '  const finishedAt = Date.now(); updateAutomationTask(task.id, { lastRunAt: finishedAt, nextRunAt: computeNextRunAt(task, finishedAt) })',
  '  const log: AutomationRunLog = { id: `log-${finishedAt}`, taskId: task.id, taskName: task.name, taskType: task.taskType, keyword: task.keyword, status, message, startedAt, finishedAt }',
  '  appendAutomationLog(log); return log',
  '}',
  'let schedulerTimer: number | undefined; let runningTaskId: string | undefined',
  'export const startAutomationScheduler = (onRun?: (log: AutomationRunLog) => void) => { if (typeof window === \"undefined\" || schedulerTimer) return; const tick = async () => { if (runningTaskId) return; const task = getDueAutomationTasks()[0]; if (!task) return; runningTaskId = task.id; try { onRun?.(await runAutomationTask(task)) } finally { runningTaskId = undefined } }; void tick(); schedulerTimer = window.setInterval(() => { void tick() }, 15000) }',
  'export const stopAutomationScheduler = () => { if (schedulerTimer) { window.clearInterval(schedulerTimer); schedulerTimer = undefined } }',
].join('\n')

fs.writeFileSync(path.join(automationDir, 'e-seal.ts'), eSeal, 'utf8')
fs.writeFileSync(path.join(automationDir, 'business-flow.ts'), businessFlow, 'utf8')
fs.writeFileSync(path.join(automationDir, 'scheduler.ts'), scheduler, 'utf8')
fs.writeFileSync(path.join(automationDir, 'index.ts'), "export * from './e-seal'\nexport * from './business-flow'\nexport * from './scheduler'\n", 'utf8')
console.log('written ai-automation services')
